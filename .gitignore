# Environment variables (secrets) — never commit actual values
.env
.env.*
!.env.example

# OS
.DS_Store

# Cache local del CLI de Salesforce. Se regenera solo, borrarlo no sirve.
.sf/

# Dependencies
node_modules/
env
